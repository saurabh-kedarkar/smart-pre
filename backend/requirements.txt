# ─── Core ───
fastapi
uvicorn[standard]
websockets
httpx
python-dotenv

# ─── Data & Analysis ───
numpy
pandas
ta
scikit-learn

# ─── NLP / Sentiment ───
vaderSentiment

# ─── Utilities ───
aiohttp
python-dateutil
pydantic

# ─── ML / Deep Learning (OPTIONAL — only install locally) ───
# torch
# transformers
# These are too large for Render free tier (2GB+).
# The app works perfectly without them using heuristic predictions.
# Install locally with: pip install torch transformers
